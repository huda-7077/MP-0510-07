export interface PaginationQueryParams {
  take: number;
  page: number;
  sortBy: string;
  sortOrder: string;
}
export interface paginationQueryParams {
  take: number;
  page: number;
  sortBy: string;
  sortOrder: string;
}